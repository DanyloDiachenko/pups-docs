export interface AuthorizationFormProps {
    language: string;
    strings: {
        auth: {
            signIn: string;
            signUp: string;
            email: string;
            password: string;
            submit: string;
        };
    };
}
