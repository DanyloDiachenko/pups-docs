export interface ChangePasswordFormProps {
    language: string;
    strings: {
        profile: {
            changePassword: {
                newPassword: string;
                submitNewPassword: string;
                submit: string;
            };
        };
    };
}
