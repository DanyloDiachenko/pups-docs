export interface SupportFormProps {
    strings: {
        support: {
            feeback: {
                yourName: string;
                yourEmail: string;
                topic: string;
                message: string;
                submit: string;
            };
        };
    };
}
